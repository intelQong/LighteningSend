{
  "name": "lighteningsend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test.mjs",
    "sim": "node sim.mjs",
    "build": "node build.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=lighteningsend"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "wrangler": "^4.0.0"
  }
}
